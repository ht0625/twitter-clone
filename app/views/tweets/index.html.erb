<h1>Tweet</h1>
<%= link_to '新規投稿', new_tweet_path %>
<table>
  <tr>
    <th>一覧</th>
  </tr>
<% @tweets.each do |tweet| %>
  <tr>
    <td><%= tweet.content %></td>
    <td><%= link_to '詳細', tweet_path(tweet.id) %></td>
  </tr>
<% end %>
</table>
